<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sportsfit.shop.repository.MemberRepository">

    <!-- 회원 저장 -->
    <insert id="save" parameterType="com.sportsfit.shop.vo.Member">
        INSERT INTO member (name, email, password, address, del, social, cart_id)
        VALUES (#{name}, #{email}, #{password}, #{address}, #{del}, #{social}, #{cartId})
    </insert>

    <!-- 이메일로 회원 조회 -->
    <select id="findByEmail" parameterType="string" resultType="com.sportsfit.shop.vo.Member">
        SELECT * FROM member WHERE email = #{email}
    </select>

    <!-- ID로 회원 조회 -->
    <select id="findById" parameterType="long" resultType="com.sportsfit.shop.vo.Member">
        SELECT * FROM member WHERE member_id = #{id}
    </select>

    <!-- 회원 업데이트 -->
    <update id="update" parameterType="com.sportsfit.shop.vo.Member">
        UPDATE member
        SET name = #{name},
            email = #{email},
            password = #{password},
            address = #{address},
            del = #{del},
            social = #{social},
            cart_id = #{cartId}
        WHERE member_id = #{id}
    </update>

    <!-- 회원 삭제 -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM member WHERE member_id = #{id}
    </delete>

    <!-- 모든 회원 조회 -->
    <select id="findAll" resultType="com.sportsfit.shop.vo.Member">
        SELECT * FROM member
    </select>

    <!-- 비밀번호와 소셜 상태 업데이트 -->
    <update id="updatePasswordAndSocial">
        UPDATE member
        SET password = #{password},
            social = false
        WHERE email = #{email}
    </update>

</mapper>
